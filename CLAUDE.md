# CLAUDE.md

This file provides guidance to Claude Code (claude.ai/code) when working with code in this repository.

## Project Overview

This is a minimal Node.js project setup with Claude Code integration. The project currently contains only basic configuration files.

## Environment Setup

- Node.js project with CommonJS module system
- Anthropic API key stored in `.env` file
- Dependencies: `@anthropic-ai/claude-code` (v2.0.34)

## Development Commands

```bash
# Install dependencies
npm install

# Run tests (not configured yet)
npm test
```

## Project Structure

Currently minimal - the project has no source files yet. This is a fresh setup ready for development.

## Configuration

- **package.json**: Main project configuration
- **.env**: Contains `ANTHROPIC_API_KEY` for Claude Code authentication
